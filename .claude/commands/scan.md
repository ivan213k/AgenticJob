---
description: Scan and summarize job listings (default 10)
argument-hint: [count]
---

Invoke the **scan-jobs** skill to list summarized jobs. Use `$ARGUMENTS` as the number of jobs to
return; if empty, default to 10. If no profile exists, ask the user to run `/setup-profile` first.
