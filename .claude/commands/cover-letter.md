---
description: Generate a cover letter for a selected job
argument-hint: <job#>
---

Invoke the **cover-letter** skill for the job selected in `$ARGUMENTS` (the number from `/scan`).
Produce a cover letter into `output/`. If no profile exists, ask the user to run `/setup-profile` first.
